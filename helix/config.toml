theme = "rose_pine"

[editor]
color-modes = true
bufferline = "multiple"

[editor.statusline]
center = ["file-type"]
right = [
  "diagnostics",
  "selections",
  "separator",
  "position",
  "position-percentage",
  "file-encoding",
]

[keys.normal]
"r" = "replace_with_yanked"
"R" = "replace"
"ß" = "split_selection_on_newline"
"…" = "flip_selections"

[keys.insert]
"j" = {"k" = "normal_mode"}

[keys.select]
"…" = "flip_selections"
